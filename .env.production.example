# ملف إعدادات البيئة للنشر الخارجي
# انسخ هذا الملف إلى .env وعدل القيم حسب خادمك

# بيئة التشغيل (production للخادم الخارجي)
NODE_ENV=production

# رابط قاعدة البيانات PostgreSQL
# استبدل القيم التالية بإعدادات خادمك:
# - username: اسم مستخدم قاعدة البيانات
# - password: كلمة مرور قوية
# - host: عنوان الخادم (localhost للخادم المحلي)
# - port: منفذ PostgreSQL (5432 افتراضي)
# - database_name: اسم قاعدة البيانات
DATABASE_URL=postgresql://qortoba_user:YourStrongPassword123!@localhost:5432/qortoba_supplies

# منفذ تشغيل التطبيق
PORT=5000

# مفتاح الجلسة السري (32 حرف على الأقل)
# أنشئ مفتاح جديد قوي لخادمك
SESSION_SECRET=your_very_secure_session_secret_minimum_32_characters_long_change_this

# مفتاح DeepSeek API للذكاء الاصطناعي (اختياري)
# إذا كنت تريد استخدام ميزة الكشف عن المكررات بالذكاء الاصطناعي
DEEPSEEK_API_KEY=your_deepseek_api_key_here

# إعدادات إضافية للأمان (اختياري)
# ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
# RATE_LIMIT_WINDOW_MS=900000
# RATE_LIMIT_MAX_REQUESTS=100

# إعدادات النسخ الاحتياطية (اختياري)
# BACKUP_RETENTION_DAYS=30
# BACKUP_PATH=/path/to/backup/directory

# إعدادات SMTP للإيميل (اختياري)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@domain.com
# SMTP_PASS=your-app-password
# FROM_EMAIL=noreply@yourdomain.com