[phases.setup]
nixPkgs = ['nodejs_20', 'npm-9_x']

[phases.install]
cmds = ['npm install --legacy-peer-deps']

[phases.build]
cmds = ['npm run build', 'ls -la dist/', 'ls -la dist/public/']

[start]
cmd = 'node dist/index.js'

[variables]
NODE_ENV = 'production'