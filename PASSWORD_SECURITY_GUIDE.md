# دليل أمان كلمات المرور - نظام قرطبة للتوريدات

## كيفية عمل تشفير كلمات المرور في النظام

### تشفير bcrypt (غير قابل للفك)
النظام يستخدم خوارزمية **bcrypt** لتشفير كلمات المرور، وهي:
- ✅ آمنة جداً
- ✅ مقاومة للهجمات
- ❌ غير قابلة لفك التشفير (by design)
- ✅ تسمح بالتحقق من صحة كلمة المرور

## لماذا لا يمكن "فك تشفير" كلمات المرور؟

### الفرق بين التشفير والـ Hashing:
- **التشفير**: يمكن فكه بمفتاح سري
- **Hashing**: عملية أحادية الاتجاه لا يمكن عكسها

bcrypt هو نوع من الـ hashing، لذلك:
```
كلمة المرور الأصلية → bcrypt hash
"admin123" → "$2b$10$atHb3PJhWHlNLcG88UfOluAEpNBO3W6bqona22dDkvU8hTHa9AXzC"
```

## الحلول المتاحة

### 1. إعادة تعيين كلمة المرور
```bash
# تحديث كلمة مرور أي مستخدم
curl -X POST "http://localhost:5000/api/reset-user-password" \
  -H "Content-Type: application/json" \
  -d '{
    "username": "it_manager",
    "newPassword": "newpassword123"
  }'
```

### 2. كلمات المرور الافتراضية المعروفة
| المستخدم | كلمة المرور الحالية |
|----------|---------------------|
| admin | admin123 |
| it_manager | it123456 |
| tech_admin | tech123456 |

### 3. إنشاء مستخدمين جدد
```bash
# إنشاء مستخدم جديد بكلمة مرور معروفة
curl -X POST "http://localhost:5000/api/test-user-create" \
  -H "Content-Type: application/json" \
  -d '{
    "username": "new_user",
    "password": "known_password",
    "fullName": "مستخدم جديد",
    "role": "data_entry"
  }'
```

## أفضل الممارسات الأمنية

### كلمات المرور القوية:
- 8 أحرف على الأقل
- مزيج من الأحرف والأرقام
- تجنب المعلومات الشخصية
- تغيير دوري

### أمثلة على كلمات مرور قوية:
- `Qurtoba2025!`
- `Admin@Supply123`
- `Manager#Tech789`

## مراقبة الأمان

### تسجيل العمليات:
- ✅ تسجيل محاولات تسجيل الدخول
- ✅ تسجيل تحديث كلمات المرور
- ✅ تتبع النشاط غير المعتاد

### الحماية من الهجمات:
- Rate limiting لمحاولات تسجيل الدخول
- Hash قوي مع salt عشوائي
- تشفير البيانات الحساسة في النقل

## في حالة نسيان كلمة المرور

### الخطوات:
1. استخدم API إعادة تعيين كلمة المرور
2. أو اتصل بمدير النظام
3. أو أنشئ مستخدم جديد إذا لزم الأمر

### تذكير مهم:
**لا يمكن استرجاع كلمات المرور الأصلية من النظام**
هذا ليس عيباً، بل ميزة أمنية مهمة!